<!-- What changed and why. Link the issue with "Closes #123". -->

- [ ] `version` bumped in `package.json`
- [ ] Ran `pnpm generate:types && pnpm generate:importmap` if a collection, global or admin component changed
- [ ] New env vars added to `src/env.ts`, `.env.example` and the CI `env` block
- [ ] Checked in a browser if this changes what a reader or an editor sees
